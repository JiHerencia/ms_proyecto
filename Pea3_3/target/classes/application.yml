server:
  port: 8093
  
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pea
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Leyenda,15
    
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        show_sql: true
        use_sql_comments: false
        format_sql: false
        
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"